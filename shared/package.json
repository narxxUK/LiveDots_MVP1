{
  "name": "@livedots/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for LiveDots",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "zod": "^3.21.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "@types/jest": "^29.5.0",
    "eslint": "^8.42.0"
  }
}